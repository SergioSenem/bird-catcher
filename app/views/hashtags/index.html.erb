<h1>Minhas hastags:</h1>

<% if @user.hashtags.count > 0 %>

<table>

<tr>
	<th>Hashtag</th>
	<th></th>
</tr>

<% @user.hashtags.each do |hashtag| %>

<tr>
	<td><%= hashtag.value %></td>
	<td><%= button_to 'Delete Hashtag', [@user, hashtag], 
		method: :delete,
		data: { confirm: 'Are you sure?' } %></td>
</tr>

<% end %>

</table>

<% else %>

<p>Não há hashtags cadastradas!</p>

<% end %>